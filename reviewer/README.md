Run with
```
python app.py --parquet_path attack.parquet
```
You can forward ssh ports to the client computer if you are running this on a remote connection.

The application saves a new "accepted" column in the same file.